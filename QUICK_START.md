# Sample to run distribution

```bash
    # download all data from Github
    yarn load

    # generate graph
    yarn start

    # connect to localhost:6006
    # enable all accounts in the UI 
    # click on "Save to Ledger"

    # run ANY tokens distribution
    yarn grain
```
